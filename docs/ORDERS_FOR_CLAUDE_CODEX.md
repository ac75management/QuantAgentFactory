# ÓRDENES INMEDIATAS — Claude + Codex (leer ya)

Alexander se retira por ahora. **No pregunten si cerrar el proyecto.**

## 1. Hechos

- Grok dejó la auditoría en el **remoto** `master`:  
  `docs/archive/reviews/AUDIT_GROK_2026-09-24.md`  
  Si no está en disco → `git pull origin master` (no reescribir de memoria).
- Ustedes reportaron Experiment Registry **hecho en local** (schema, JSONL, CLI, 191 tests).  
  Si no está en GitHub remoto → **push ahora**.
- `AGENTS.md` en remoto puede estar dañado/recortado → **restaurar historial** y mantener schema + estas órdenes.

## 2. Hacer en este orden (autónomo)

1. Sync git (pull/push registry).
2. Cerrar DoD registry: backfill runs históricos + `count-trials` OK.
3. Bitácora PROJECT_STATE: registry operativo; NEXT ACTION = BLOQUE 2 standing orders.
4. BLOQUE 2 de `docs/STANDING_ORDERS_ALEXANDER_AWAY.md` (sensibilidad=trials, preflight foto, costs docs).
5. Parar en el checklist de pausa del mismo archivo.

## 3. Prohibido

OOS, bróker, cambiar gates, rescatar 001–011, IS masivos, nuevos agentes, cerrar el repo/proyecto.

## 4. Comunicación

Solo `AGENTS.md` (Hallazgos cruzados) y `PROJECT_STATE.md`.  
Detalle: `docs/STANDING_ORDERS_ALEXANDER_AWAY.md`.
